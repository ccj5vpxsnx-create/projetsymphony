{% extends 'base.html.twig' %}

{% block title %}Modifier {{ etudiant.nom }} — Gestion Stages{% endblock %}

{% block breadcrumb %}
    <li><span class="sep">/</span></li>
    <li><a href="{{ path('app_etudiant_index') }}">Étudiants</a></li>
    <li><span class="sep">/</span></li>
    <li><a href="{{ path('app_etudiant_show', {'id': etudiant.id}) }}">{{ etudiant.nom }}</a></li>
    <li><span class="sep">/</span></li>
    <li>Modifier</li>
{% endblock %}

{% block topbar_actions %}
    <a href="{{ path('app_etudiant_show', {'id': etudiant.id}) }}" class="btn btn-outline btn-sm">
        <i class="fas fa-eye"></i> Voir la fiche
    </a>
    <a href="{{ path('app_etudiant_index') }}" class="btn btn-outline btn-sm">
        <i class="fas fa-arrow-left"></i> Retour
    </a>
{% endblock %}

{% block body %}

<div class="page-header">
    <div class="page-title">
        <em>édition</em>
        {{ etudiant.nom }} {{ etudiant.prenom }}
    </div>
</div>

<div style="display:grid;grid-template-columns:1fr 300px;gap:1.5rem;align-items:start">

    <div class="card card-body">
        {{ include('etudiant/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}
    </div>

    <div style="display:flex;flex-direction:column;gap:1rem">
        <div class="card card-body danger-zone">
            <div class="danger-zone-label"><i class="fas fa-exclamation-triangle" style="margin-right:.35rem"></i>Supprimer</div>
            <p style="font-size:.78rem;color:var(--muted);margin-bottom:.85rem">Cette action est irréversible. Toutes les données associées seront supprimées.</p>
            {{ include('etudiant/_delete_form.html.twig') }}
        </div>
    </div>

</div>

{% endblock %}
