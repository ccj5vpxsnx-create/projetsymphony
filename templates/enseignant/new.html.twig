{% extends 'base.html.twig' %}
{% block title %}Ajouter Enseignant{% endblock %}
{% block body %}
<div class="card p-4">
    <h1><i class="fas fa-plus"></i> Ajouter Enseignant</h1>
    {{ include('enseignant/_form.html.twig') }}
    <a href="{{ path('app_enseignant_index') }}" class="btn btn-secondary mt-3">
        <i class="fas fa-arrow-left"></i> Retour
    </a>
</div>
{% endblock %}