{% extends 'base.html.twig' %}

{% block title %}Étudiants — Gestion Stages{% endblock %}

{% block breadcrumb %}
    <li><span class="sep">/</span></li>
    <li>Étudiants</li>
{% endblock %}



{% block body %}

<div class="page-header">
    <div class="page-title">
        <em>liste des</em>
        Enseignants
    </div>
</div>

{# ── Stats ── #}
<div class="stat-grid">
    <div class="stat-card">
        <div class="stat-label">Total enseignants</div>
        <div class="stat-value">{{ enseignants|length }}</div>
    </div>
   

</div>

{# ── Table ── #}
<div class="card">
    <table class="data-table">
        <thead>
            <tr>
                <th>#</th>
                <th><i class="fas fa-id-card" style="margin-right:.35rem;opacity:.5"></i>matricule</th>
                <th><i class="fas fa-user" style="margin-right:.35rem;opacity:.5"></i>Nom</th>
                <th>Prénom</th>
                <th style="text-align:right">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for enseignant in enseignants %}
            <tr>
                <td class="id-cell">{{ enseignant.id }}</td>
                <td>
                    <span class="badge-chip">
                        <i class="fas fa-hashtag" style="font-size:.6rem"></i>
                        {{ enseignant.matricule }}
                    </span>
                </td>
                <td style="font-weight:600">{{ enseignant.nom }}</td>
                <td style="color:var(--muted)">{{ enseignant.prenom }}</td>
                <td>
                    <div class="action-group" style="justify-content:flex-end">
                        <a href="{{ path('app_enseignant_show', {'id': enseignant.id}) }}" class="action-link view">
                            <i class="fas fa-eye"></i> Voir
                        </a>
                        <a href="{{ path('app_enseignant_edit', {'id': enseignant.id}) }}" class="action-link edit">
                            <i class="fas fa-pen"></i> Modifier
                        </a>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr class="empty-row">
                <td colspan="5">
                    <i class="fas fa-inbox"></i>
                    Aucun enseignant enregistré.
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>

{% endblock %}
